# 設計書 — タスク管理ツール復元版（ローカルPython・最小構成）

- 作成日: 2026-09-22
- ステータス: **実装済み**（2026-09-23、Claude。レビューで§5.1のmode決定ロジックの
  乖離を修正した上で実装。詳細は HANDOFF.md 参照）
- 由来: `gas-projects/project2`（GAS）の復元。設計の正本は
  `docs/MODEMAP_RESTORATION.md` / `docs/SPECIFICATION.md`。
- 方針: **最小構成で先行実装し、将来 WebUI を載せられる構造にする。**

---

## 1. 目的・背景

失われた project2（GAS＋Cloud SQL）の機能を、ローカルPythonで復元する。

復元対象の機能は project2 のみ:

> 原稿ブックの `lineart` シート（ページ×工程マトリクス）を読み、
> モード別の重み（modemapマスタ）で**未着手セルの残作業時間**を集計し、
> plot（話）×カテゴリ別の見積を出力する。

project1 は参考扱い（モード体系の理解のため）とし、復元に含めない。

## 2. スコープ

### 2.1 今回やること（最小構成）

- xlsx 入力（元の原稿形式にならう。ユーザーが手で編集する）
- マスタは **CSV**（`restoration/modemap.csv` を流用・運用更新する）
- CLI 出力: plot×カテゴリの残作業時間（時間単位、小数1桁）＋合計
- 純粋関数コア＋薄いCLIラッパの2層構造（将来のWebUI再利用前提）

### 2.2 今回やらないこと（将来拡張）

| 項目 | 将来対応の想定 |
|---|---|
| WebUI | コア関数をそのまま呼ぶ `web.py` を追加（Flask/FastAPI＋HTMX等） |
| DuckDB化 | マスタ読込を `master.py` 1箇所に閉じ込めてあり、IFを変えず差し替え可能 |
| xlsx への書き戻し（analysis シート） | CLIオプション or WebUI から。元GASは `analysis!A3:B` へ書いていた |
| color / parts シートの集計 | 元GASも未実装。カテゴリ設計は対応済み |
| キャリブレーション支援 | WebUI での編集画面が自然 |

## 3. 全体構成

```
task_tracking/
├── 設計書.md                      ← 本ファイル
├── restoration/
│   └── modemap.csv                ← 重みマスタ（唯一のデータ源。運用で更新）
├── taskman/                       ← 復元アプリ（Pythonパッケージ）
│   ├── __init__.py
│   ├── master.py                  # マスタ読込（CSV）
│   ├── sheet.py                   # xlsx lineart 読込
│   ├── analyze.py                 # 集計ロジック（純粋関数）
│   └── cli.py                     # argparse＋表示
└── spreadsheets/
    └── manuscript.xlsx            ← 入力例（この形式でユーザーが作成）
```

依存: **openpyxl のみ**（既存環境で使用確認済み）。あとは標準ライブラリ。
pandas / DuckDB / その他の外部ライブラリは使わない（最小構成）。

## 4. データ設計

### 4.1 入力 xlsx 形式（元の原稿 lineart にならう）

| 行 | 内容 |
|---|---|
| 1行目 | ヘッダ。`page, plot, mode, scene, title,` の後に工程カテゴリ列（`sketch, head, body, cloth, item, background, base, erotic, shadow, back, front, mood, onomatopeia, dialog, ...`） |
| 2行目 | section 行（表示用。アプリは無視） |
| 3行目〜 | データ。1行=1ページ |

セル値の语义（元GAS `data.js:73` と同一）:

| セルの状態 | 意味 |
|---|---|
| 空白（None or ''） | **未着手** → その工程カテゴリの重み（分）を加算 |
| `finished` / `not use` / `pending` / `working` / `partial` 等、空白以外の任意の値 | 着手済 or 対象外 → 加算しない |

- `mode` 列の値はマスタに存在するモード名（必須。`standard`/`battle` 等）。
  マスタに無いモードの行は**警告を出してスキップ**する（元GASは落ちたが、復元版は寛容に）。
- `plot` が空白の行はスキップ（元GASと同一。警告なしで黙って飛ばすかは実装時判断→黙って飛ばす）。
- 集計対象カテゴリ = **シートのヘッダ列 ∩ マスタのカテゴリ列** の共通部分。
  （例: シートに `line`/`effect`/`erotic_effect` 列が無ければ集計から自動除外。
  逆にシートだけに存在する `speaking`/`nombre` 等もマスタに無いので除外。）

### 4.2 マスタ CSV 形式

`restoration/modemap.csv`（復元済み）をそのまま使用:

```
mode,sketch,head,body,cloth,item,background,base,shadow,erotic,line,effect,erotic_effect,onomatopeia,front,back,dialog,mood
standard,60,40,40,30,25,40,20,15,25,15,15,20,10,10,10,10,10
battle,70,45,45,35,30,55,25,20,30,20,20,25,15,15,15,10,12
...
```

- 1行目ヘッダ。1列目=mode名、2列目以降=カテゴリ名（分・整数値）。
- カテゴリ列は17列標準だが、**ヘッダに無いカテゴリがあっても読み込める**構造
  （増減は許容。集計は §5 の5群マッピングで拾えるものだけ行う）。
- 値はキャリブレーション前の仮置き。運用で上書き更新する（手順は
  MODEMAP_RESTORATION.md §5）。

### 4.3 内部データ構造

```python
# master.py
Weights = dict[str, dict[str, int]]   # {mode: {category: minutes}}

# sheet.py
Row = dict[str, object]               # {"page":1, "plot":"海難事故救出", "mode":"standard", ..., "sketch":"finished", "head":None, ...}
SheetData = tuple[list[Row], list[str]]  # (データ行, カテゴリ列名リスト)

# analyze.py
Result = dict[str, dict[str, int]]    # {plot: {group: minutes}}（5群）
GROUPS = ("sketch", "lineart", "object", "illustrate", "effect")
GROUP_MAP = {                         # data.js:85 summarize() と同一
  "sketch": ("sketch",),
  "lineart": ("head", "body", "cloth"),
  "object": ("item", "background"),
  "illustrate": ("base", "shadow", "erotic", "line"),
  "effect": ("effect", "erotic_effect", "onomatopeia", "front", "back", "dialog", "mood"),
}
```

## 5. 処理設計

### 5.1 アルゴリズム（data.js ベース、mode適用のみ意図的に変更）

**元コードの実際の挙動**: data.js の `getWorkTime()` は
`dict = modemap[workdata[0]['mode']]` — **そのplotの先頭行（シート上で
最初に出現した行）のmodeだけ**で重みテーブルを1つ選び、plot内の全行に
同一の重みを適用していた。行ごとのmode値（2行目以降）は一切見ない。

実データ検証で 7 plot中4つ（砕月暴走・会議・万博 蛸蜥蜴・事件後）が
plot内でmode混在していることを確認。これはページ単位でmodeが変わりうる
工程管理ツールとして妥当でなく、2014年当時の実装上の見落としと判断。
**→ 利用者確認の上、"行ごとに自分のmodeで重みを引く" 方式に意図的に変更する**
（§10 差分リストに追加）。plot内でmode混在があっても、各行は自分の行の
mode列の値だけを見て加算する。

plotのグルーピングは「plot名で全行をフィルタ」（`data.filter(row.plot==p)`と
同一）で行う。plotは同一シート内で非連続に再出現しうるため、連続ブロックの
分割では実装しないこと。

```python
def analyze(rows, categories, weights) -> Result:
    result = {}
    skipped = {}  # (plot, mode) -> スキップ行数
    for plot in plots(rows):                    # 出現順、plot=='' は除外（data.js:104-106）
        plot_rows = [r for r in rows if r["plot"] == plot]   # 非連続再出現に対応

        acc = {cat: 0 for cat in categories}
        for row in plot_rows:
            w = weights.get(row["mode"])        # ★行ごとに自分のmodeを使う（元コードと異なる）
            if w is None:
                skipped[(plot, row["mode"])] = skipped.get((plot, row["mode"]), 0) + 1
                continue
            for cat in categories:              # シート∧マスタ の共通カテゴリのみ
                if cat in w and row[cat] in (None, ""):   # data.js:73 work[k]=='' と同一语义
                    acc[cat] += w[cat]          # 分を加算
        result[plot] = summarize(acc)           # data.js:85 と同一の5群集約
    return result
```

（訂正: 元コードは未定義モードでも例外を投げない。`for(k in undefined)` は
JSでは無害で、結果はNaNになり出力フィルタ `value>0` で静かに消えるだけ。
「元はTypeError」という記述は誤りだったため削除。警告+skipという復元版の
方針自体はそのまま採用——クラッシュしないより明示的で分かりやすいため。）

出力は 分→時間変換（`/60`、小数1桁。`constructAnalytics` data.js:120 と同一）
し、**0より大きい行のみ**出力（data.js:123 の `filter(value[1] > 0)` と同一）。

### 5.2 元コードとの対応表

| 元GAS (project2) | 復元版 | 変更点 |
|---|---|---|
| `getModemapArray()` Jdbc+SQL | `master.load_master(path)` CSV読込 | JDBC→CSV |
| `modemap = getModemap()` グローバル即接続 | CLI呼出時に遅延ロード | import時副作用を排除 |
| `getDic(...).arrayByRow()` | `sheet.load_sheet(path, sheet)` openpyxl | 同型の行dict化を再実装 |
| `[e for each (e in data)]` 旧構文 | 標準ループ | V8で構文エラーだったものを解消 |
| `getWorkTime` / `summarize` | `analyze.analyze` | 语义同一。未定義モードはwarn+skipに寛容化 |
| `constructAnalytics` | 分→h変換+0フィルタ | 同一（小数1桁） |
| `writeAnalytics` → analysis!A3:B | CLI標準出力へ整形 | 書き戻しは将来拡張 |
| ssID ハードコード | CLI引数 | |

## 6. CLI仕様

### 6.1 コマンド

```
python -m taskman analyze <input.xlsx> [--sheet lineart] [--master restoration/modemap.csv]
```

| 引数/オプション | 既定値 | 説明 |
|---|---|---|
| `input.xlsx`（必須） | ― | 入力ブック。原稿形式（§4.1） |
| `--sheet` | `lineart` | 対象シート名 |
| `--master` | `restoration/modemap.csv` | 重みマスタCSV |

### 6.2 出力例

```
$ python -m taskman analyze spreadsheets/manuscript.xlsx
plot            category      rest(h)
--------------  ------------  ------
海難事故救出    sketch             0.8
海難事故救出    lineart            1.2
砕月暴走        sketch             2.1
...
--------------------------------------
total                             25.4

warning: mode 'xxx' not in master (skipped 2 rows)
```

- 表は plot 出現順 × 5群の固定順（sketch, lineart, object, illustrate, effect）。
  0 の行は出力しない（元仕様どおり）。
- 合計行はカテゴリ群を問わず全行の合計（元の analysis!B2 `=SUM` に相当）。
- 警告（未定義モード行のスキップ等）は stdout の表**後**、または stderr に出力。

### 6.3 終了コード・エラー

| 状況 | 終了コード |
|---|---|
| 正常（警告があっても） | 0 |
| 引数エラー | 2（argparse既定） |
| ファイル不存在・フォーマット不正（ヘッダに mode/plot 不在等） | 1＋stderrにメッセージ |

## 7. 実装手順

1. `taskman` パッケージ骨格（`__init__.py`）
2. `master.py`: CSV読込＋バリデーション（整数変換、空行スキップ）
3. `sheet.py`: openpyxlで行dict化。ヘッダからカテゴリ列（§4.1のメタ5列以外）を特定
4. `analyze.py`: §5.1 アルゴリズム（純粋関数、I/Oなし）
5. `cli.py`: argparse＋表整形出力
6. 検証: `spreadsheets/manuscript.xlsx` で実行し、フォーマットと網羅性を確認
   - ※ 値がスナップショットの旧 analysis シートと一致しないのは正しい
     （重みは仮置き＆lineartデータは旧出力以降に更新済み）。一致はキャリブレーション後の話。
7. 動作確認後、`--sheet` で concept ブックの lineart も試す（行の開始位置が
   原稿=3行目/concept=4行目で異なるため、必要なら `--data-start-row` オプションを追加検討）

## 8. 検証計画（テストコードは書かない方針）

- スナップショット xlsx 実データでのスモーク実行（§7-6）
- 確認観点:
  - 全 plot が出現順に出力される
  - `finished` セルは加算されず、空白セルのみ加算される（lineart 3〜5行目の
    `head/body/cloth=finished`、`item=None` 等で目視確認可能）
  - 未定義モード行で警告が出て処理が継続する
  - カテゴリ群の合計が合理範囲（1ページあたり数時間程度）

## 9. 将来拡張への備え（設計上の決定事項）

| 決定 | 理由 |
|---|---|
| コア（analyze.py）はI/O完全分離の純粋関数 | WebUI からそのまま呼べる |
| マスタ読込は `master.py` の単一関数に閉じる | DuckDB/スプレッドシート化の差し替え点を1箇所に限定 |
| 行表現は `dict[str, object]` | シート列の増減に強い。位置依存（列番号固定）にしない |
| 5群マッピングは定数として analyze.py に保持 | 元の summarize 语义を正本にする |

## 10. オリジナルから意図的に変えた点（差分リスト）

1. JDBC/Cloud SQL → CSV（インフラ不要。値は仮置きで運用キャリブレーション）
2. スクリプト読み込み時の即DB接続 → 遅延ロード
3. 未定義モードで結果がNaNになり静かに消える → 警告＋該当行スキップ
4. xlsx 書き戻し → CLI 標準出力（書き戻しは将来）
5. ハードコード ssID/シート名 → CLI 引数
6. Rhino 専用 `for each` 構文 → 標準構文
7. **mode適用単位を「plotの先頭行のみ」→「行ごと」に変更**（元コードの
   `modemap[workdata[0]['mode']]` は実データで4/7 plotに影響する見落としと
   判断し、利用者確認の上で修正。§5.1参照）
